"""figmark — turn a PDF into Markdown with first-class AI figure descriptions.

figmark extracts the text of a PDF and replaces every image and vector diagram
with an AI-generated description, producing a single coherent Markdown document
where figures are interpreted inline rather than dropped.
"""

from __future__ import annotations

__version__ = "0.3.0"
